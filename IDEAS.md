# SmartTask360 — Ideas & Future Improvements

**Цель:** Хранение идей и предложений для развития системы после MVP.

**Статусы:**
- 💡 **Idea** — сырая идея, требует обсуждения
- 🔍 **Research** — требуется исследование технологий
- 📋 **Planned** — принято в бэклог, будет реализовано
- ✅ **Done** — реализовано
- ❌ **Rejected** — отклонено с причиной

---

## 🤖 RAG System — AI-ассистент для стратегических сессий

**Статус:** 💡 Idea
**Дата:** 2026-01-02
**Автор:** User

### Описание

Многопользовательский режим обсуждения стратегии и задач на основе документов:
- Протоколы встреч
- Маркетинговые отчеты
- Стратегические планы
- OKR документы
- BSC метрики

### Use Cases

1. **"Обсудим стратегию Q1"**
   - AI анализирует все релевантные документы
   - Предлагает инсайты на основе прошлых протоколов
   - Участники задают вопросы, AI отвечает с цитатами из документов

2. **"Почему мы не достигли OKR по продажам?"**
   - AI находит связанные задачи, комментарии, отчеты
   - Показывает корреляции с другими метриками
   - Предлагает гипотезы на основе паттернов

3. **"Создай задачи на основе протокола встречи"**
   - AI парсит протокол
   - Выделяет action items
   - Создает задачи с привязкой к ответственным

### Технологии

#### Backend
- **pgvector** — хранение эмбеддингов документов в PostgreSQL
- **Anthropic Claude API** — генерация ответов (уже используется)
- **LangChain** или **LlamaIndex** — RAG pipeline
- **Celery** — фоновая обработка документов (создание эмбеддингов)
- **Redis** — кэш для частых запросов

#### Embedding модели
- **Voyage AI** — лучшие эмбеддинги для русского (2024)
- **Cohere Embed v3** — multilingual
- **OpenAI text-embedding-3-large** — fallback

#### Векторный поиск
- **pgvector** (PostgreSQL extension) — хранение в основной БД
- Альтернатива: **Qdrant** / **Weaviate** (если pgvector не хватит)

#### Document Processing
- **pypdf** (уже в requirements) — PDF
- **python-docx** (уже в requirements) — Word
- **unstructured** — парсинг сложных документов
- **pptx** — PowerPoint презентации

### Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│  User: "Обсудим стратегию Q1 на основе последних отчетов"   │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  Frontend: Чат-интерфейс (React + WebSocket)                │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  Backend: RAG Service                                        │
│  1. Создает embedding вопроса                               │
│  2. Ищет похожие документы в pgvector                       │
│  3. Формирует контекст для Claude                           │
│  4. Отправляет промпт в Anthropic API                       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  PostgreSQL (pgvector)                                       │
│  SELECT content FROM documents                               │
│  ORDER BY embedding <=> query_embedding                      │
│  LIMIT 5                                                     │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  Claude API: Генерирует ответ с цитатами                    │
└─────────────────────────────────────────────────────────────┘
```

### Структура БД (дополнительные таблицы)

```sql
-- Документы с эмбеддингами
CREATE TABLE rag_documents (
    id UUID PRIMARY KEY,
    title VARCHAR(500),
    content TEXT,
    file_path VARCHAR(1000),  -- MinIO path
    embedding vector(1536),   -- pgvector
    metadata JSONB,           -- {author, date, department, doc_type}
    created_at TIMESTAMPTZ,
    INDEX idx_embedding USING ivfflat (embedding vector_cosine_ops)
);

-- Чанки документов (для длинных документов)
CREATE TABLE rag_chunks (
    id UUID PRIMARY KEY,
    document_id UUID REFERENCES rag_documents(id),
    chunk_index INT,
    content TEXT,
    embedding vector(1536),
    INDEX idx_chunk_embedding USING ivfflat (embedding vector_cosine_ops)
);

-- История RAG-диалогов
CREATE TABLE rag_conversations (
    id UUID PRIMARY KEY,
    user_id UUID REFERENCES users(id),
    project_id UUID REFERENCES projects(id),
    title VARCHAR(500),
    created_at TIMESTAMPTZ
);

CREATE TABLE rag_messages (
    id UUID PRIMARY KEY,
    conversation_id UUID REFERENCES rag_conversations(id),
    role VARCHAR(20),  -- 'user' | 'assistant'
    content TEXT,
    sources JSONB,  -- [{document_id, chunk_id, relevance_score}]
    created_at TIMESTAMPTZ
);
```

### Этапы реализации

**Phase 1: Document Ingestion (1-2 недели)**
- [ ] Загрузка документов через UI
- [ ] Парсинг (PDF, DOCX, TXT)
- [ ] Chunking (разбиение на части по 500 токенов)
- [ ] Создание эмбеддингов (фоновая задача)
- [ ] Сохранение в pgvector

**Phase 2: Vector Search (1 неделя)**
- [ ] Эндпоинт: "Найти похожие документы"
- [ ] Фильтрация по метаданным (дата, автор, тип)
- [ ] Ранжирование результатов

**Phase 3: RAG Pipeline (2 недели)**
- [ ] Эндпоинт: "Задать вопрос"
- [ ] Формирование контекста из топ-5 документов
- [ ] Интеграция с Claude API
- [ ] Стриминг ответа (WebSocket)

**Phase 4: Multi-user Conversations (1 неделя)**
- [ ] Общие чаты по проектам
- [ ] Real-time обновления (WebSocket)
- [ ] Сохранение истории

**Phase 5: Advanced Features (2-3 недели)**
- [ ] Создание задач из диалога
- [ ] Автоматическое тегирование документов
- [ ] Графы знаний (связи между документами)
- [ ] Аналитика: какие документы чаще используются

### Риски

- **Стоимость embeddings:** ~$0.13 за 1M токенов (OpenAI) или ~$0.10 (Voyage)
- **Размер БД:** 1000 документов × 1536 размерность × 4 bytes = ~6 MB (приемлемо)
- **Скорость поиска:** pgvector медленнее специализированных векторных БД при >1M документов

### Альтернативы

1. **Без pgvector:**
   - **Qdrant** (Rust, быстрее, но отдельный сервис)
   - **Weaviate** (GraphQL API, хорошо для знаниевых графов)
   - **Pinecone** (managed, но платный)

2. **Без кастомного RAG:**
   - **LangChain** + **Chroma** (быстрый старт, но меньше контроля)

### Оценка сложности

- **Backend:** 5-6 недель (с тестами)
- **Frontend:** 2-3 недели (чат UI)
- **DevOps:** 1 неделя (настройка pgvector, Redis, Celery)

**Итого:** ~8-10 недель (2-2.5 месяца)

---

## 🎨 UI Library: Ant Design vs Tailwind + Headless UI

**Статус:** 🔍 Research
**Дата:** 2026-01-02
**Автор:** User

### Текущее решение: Tailwind CSS + Headless UI

**Плюсы:**
- ✅ Полный контроль над дизайном
- ✅ Легковесность (нет лишних компонентов)
- ✅ Современный стек (используется в проекте)

**Минусы:**
- ❌ Нужно писать компоненты с нуля
- ❌ Медленнее разработка UI

### Ant Design (antd)

**Плюсы:**
- ✅ **Готовые компоненты:** Table, Form, Modal, DatePicker, Tree (для иерархии задач!)
- ✅ **Enterprise-ready:** используется в крупных проектах (Alibaba, Tencent)
- ✅ **TypeScript support:** отличная типизация
- ✅ **Документация:** подробная, с примерами
- ✅ **Темизация:** легко кастомизировать цвета
- ✅ **i18n:** поддержка русского из коробки

**Минусы:**
- ❌ **Размер бандла:** ~600 KB (с tree-shaking ~200 KB)
- ❌ **Opinionated дизайн:** сложно радикально изменить стиль
- ❌ **Конфликт с Tailwind:** нужно аккуратно совмещать

### Рекомендация: **Гибридный подход**

**Вариант 1: Ant Design для сложных компонентов**
```typescript
import { Table, Tree, DatePicker } from 'antd';
// Используем antd для:
// - Таблицы задач (сортировка, фильтрация, пагинация)
// - Tree для иерархии задач
// - DatePicker для дедлайнов
// - Form для сложных форм

// Tailwind для:
// - Layout (flex, grid)
// - Простые компоненты (кнопки, карточки)
// - Кастомные стили
```

**Вариант 2: Shadcn/ui (Radix UI + Tailwind)**
```bash
npm install @radix-ui/react-* class-variance-authority
```
- Компоненты на базе Radix UI (headless)
- Стилизованы Tailwind
- Копируете код компонентов в проект (полный контроль)
- Легковесно, современно

### Сравнительная таблица

| Критерий | Ant Design | Shadcn/ui | Tailwind + Headless UI |
|----------|-----------|-----------|------------------------|
| **Скорость разработки** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Гибкость дизайна** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Размер бандла** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **TypeScript** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Документация** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Enterprise-готовность** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |

### Решение для SmartTask360

**Рекомендую: Shadcn/ui**

**Почему:**
1. У вас уже Tailwind в проекте
2. Shadcn/ui дает готовые компоненты + полный контроль
3. Легко кастомизировать под бренд
4. Современный, активно развивается
5. Легковесно (tree-shaking работает отлично)

**Компоненты, которые понадобятся:**
- `Table` — для списка задач
- `Dialog` — модальные окна
- `DropdownMenu` — меню действий
- `Calendar` — выбор дат
- `Tabs` — вкладки (задачи/подзадачи/комментарии)
- `Command` — command palette (как в VS Code)
- `TreeView` — иерархия задач (есть в community components)

### Действия

- [ ] Оставить решение до Phase 2 (Frontend Development)
- [ ] Когда дойдем до UI, сделать proof-of-concept с Shadcn/ui
- [ ] Если не подойдет, можно быстро переключиться на Ant Design

---

## 📋 Backlog идей

### 📱 Mobile Application

**Статус:** 💡 Idea
**Дата:** 2026-01-02
**Priority:** High (для enterprise)
**Effort:** Large (2-3 месяца)

**Описание:**
Нативное мобильное приложение на основе текущего backend API.

**Технологии:**
- **React Native** (переиспользование React компонентов)
- **Expo** (быстрая разработка и деплой)
- Альтернатива: **Flutter** (если нужна высокая производительность)

**Плюсы React Native:**
- ✅ Переиспользование логики с web (React Query, hooks)
- ✅ Один codebase для iOS и Android
- ✅ Быстрая разработка (Expo Go для тестирования)

**Что нужно учесть:**
- Offline-first подход (работа без интернета)
- Push-уведомления (Firebase Cloud Messaging)
- Камера для сканирования документов
- Голосовой ввод (см. ниже)

---

### 🎙️ Voice Notes + AI Analysis

**Статус:** 💡 Idea
**Дата:** 2026-01-02
**Priority:** Medium
**Effort:** Medium (2-3 недели)

**Описание:**
1. Пользователь наговаривает заметки голосом
2. AI транскрибирует → суммаризует → находит инсайты/противоречия
3. По результатам создаются задачи или инициативы

**Технологии:**

**Speech-to-Text:**
- **Whisper API** (OpenAI) — отличное качество для русского
- **Google Cloud Speech-to-Text** — альтернатива
- **Vosk** (open-source, локально)

**Workflow:**
```
Пользователь говорит 5 минут о проекте
    ↓
Whisper API → текстовая транскрипция
    ↓
Claude API анализирует:
  - Суммаризация (краткое содержание)
  - Инсайты (новые идеи)
  - Противоречия (конфликты с существующими задачами)
  - Action items (что нужно сделать)
    ↓
UI показывает результат + предлагает создать задачи
```

**Пример промпта для Claude:**
```
Транскрипция голосовой заметки:
"[текст]"

Проанализируй и выдели:
1. Краткое содержание (2-3 предложения)
2. Ключевые инсайты (новые идеи)
3. Противоречия с существующими задачами: [список задач]
4. Action items (конкретные действия)

Формат ответа: JSON
```

**Структура БД:**
```sql
CREATE TABLE voice_notes (
    id UUID PRIMARY KEY,
    user_id UUID REFERENCES users(id),
    audio_file VARCHAR(1000),  -- MinIO path
    transcription TEXT,
    analysis JSONB,  -- {summary, insights, contradictions, action_items}
    created_at TIMESTAMPTZ
);
```

**Интеграция:**
- Кнопка "Записать заметку" в UI
- После анализа → "Создать задачи" (одним кликом)
- История заметок (можно пересмотреть анализ)

---

### 🔍 Duplicate Task Detection

**Статус:** 💡 Idea
**Дата:** 2026-01-02
**Priority:** High (частая проблема)
**Effort:** Medium (1-2 недели)

**Описание:**
При создании задачи проверять, нет ли уже похожей по смыслу.

**Технологии:**
- **pgvector** (семантический поиск через embeddings)
- **pg_trgm** (текстовое сходство, быстрее)
- **Claude API** (финальная проверка при высоком сходстве)

**Workflow:**

**Вариант 1: Простой (pg_trgm)**
```sql
-- Быстрая проверка на текстовое сходство
SELECT id, title, similarity(title, 'Новая задача') AS score
FROM tasks
WHERE similarity(title, 'Новая задача') > 0.3
ORDER BY score DESC
LIMIT 5;
```

**Вариант 2: Продвинутый (pgvector)**
```python
# При создании задачи
new_task_embedding = get_embedding(task.title + task.description)

# Поиск похожих
similar_tasks = db.query(Task).filter(
    Task.embedding.cosine_distance(new_task_embedding) < 0.2
).limit(5)

if similar_tasks:
    # Показать предупреждение пользователю
    return {
        "warning": "Найдены похожие задачи",
        "similar": [TaskResponse.from_orm(t) for t in similar_tasks]
    }
```

**Вариант 3: Гибридный (pg_trgm + Claude)**
```python
# Шаг 1: Быстрая фильтрация (pg_trgm)
candidates = get_similar_by_text(task.title, threshold=0.3)

if len(candidates) > 0:
    # Шаг 2: Точная проверка (Claude API)
    prompt = f"""
    Новая задача: {task.title} - {task.description}

    Похожие задачи:
    {candidates}

    Это дубликаты? (yes/no/partial)
    Если да, объясни почему.
    """

    result = await claude_api.analyze(prompt)

    if result.is_duplicate:
        return {"error": "Дубликат задачи", "original": result.original_task}
```

**UI/UX:**
```
┌─────────────────────────────────────────────────────────┐
│  Создание задачи                                        │
├─────────────────────────────────────────────────────────┤
│  Название: [Настроить CI/CD pipeline__________]         │
│                                                         │
│  ⚠️  Внимание! Найдены похожие задачи:                  │
│                                                         │
│  📋 Настроить GitHub Actions (создана 2 дня назад)      │
│      Статус: В работе | Исполнитель: Иван              │
│      [Открыть] [Это та же задача]                      │
│                                                         │
│  📋 Автоматизация деплоя (создана неделю назад)         │
│      Статус: Готово | Исполнитель: Мария               │
│      [Открыть] [Связать задачи]                        │
│                                                         │
│  [Всё равно создать]  [Отмена]                         │
└─────────────────────────────────────────────────────────┘
```

**Настройки (для админа):**
- Порог сходства (0.1 - 1.0)
- Включить/выключить проверку
- Только для определенных проектов

---

### 🔗 Task Dependencies (Зависимости задач)

**Статус:** 💡 Idea
**Дата:** 2026-01-02
**Priority:** Critical (это MVP feature!)
**Effort:** Medium (1-2 недели)

**Описание:**
Задачи могут зависеть друг от друга (нельзя начать B, пока не завершена A).

**Типы зависимостей:**
1. **Finish-to-Start (FS):** Задача B начинается после завершения A
2. **Start-to-Start (SS):** B начинается вместе с A
3. **Finish-to-Finish (FF):** B завершается вместе с A
4. **Start-to-Finish (SF):** B завершается, когда начинается A (редко)

**Структура БД:**
```sql
CREATE TABLE task_dependencies (
    id UUID PRIMARY KEY,
    task_id UUID REFERENCES tasks(id),         -- Зависимая задача
    depends_on_task_id UUID REFERENCES tasks(id),  -- От какой зависит
    dependency_type VARCHAR(20),  -- 'finish_to_start' | 'start_to_start' | ...
    lag_days INT DEFAULT 0,  -- Задержка (например: через 2 дня после A)
    created_at TIMESTAMPTZ,
    UNIQUE(task_id, depends_on_task_id)  -- Нельзя дублировать зависимости
);

-- Индекс для быстрого поиска
CREATE INDEX idx_task_dependencies ON task_dependencies(task_id);
CREATE INDEX idx_depends_on_task ON task_dependencies(depends_on_task_id);
```

**Бизнес-логика:**

**Проверка при изменении статуса:**
```python
async def change_status(task_id: UUID, new_status: str):
    task = await get_task(task_id)

    # Проверка зависимостей
    if new_status == "in_progress":
        # Проверить, что все зависимости выполнены
        dependencies = await db.execute(
            select(TaskDependency)
            .where(TaskDependency.task_id == task_id)
            .where(TaskDependency.dependency_type == "finish_to_start")
        )

        for dep in dependencies:
            parent_task = await get_task(dep.depends_on_task_id)
            if parent_task.status != "done":
                raise HTTPException(
                    status_code=400,
                    detail=f"Нельзя начать задачу. Сначала завершите: {parent_task.title}"
                )

    # Обновить статус
    task.status = new_status
    await db.commit()

    # Уведомить зависимые задачи
    if new_status == "done":
        await notify_dependent_tasks(task_id)
```

**Обнаружение циклических зависимостей:**
```python
def has_circular_dependency(task_id: UUID, depends_on_id: UUID) -> bool:
    """Проверка на A → B → C → A"""
    visited = set()

    def dfs(current_id):
        if current_id == task_id:
            return True  # Найден цикл!
        if current_id in visited:
            return False
        visited.add(current_id)

        # Получить зависимости текущей задачи
        deps = get_dependencies(current_id)
        for dep in deps:
            if dfs(dep.depends_on_task_id):
                return True
        return False

    return dfs(depends_on_id)

# При создании зависимости
if has_circular_dependency(task.id, depends_on_task.id):
    raise HTTPException(400, "Обнаружена циклическая зависимость!")
```

**UI для управления зависимостями:**

**Вариант 1: Граф зависимостей (визуальный)**
```
   ┌─────────┐
   │ Task A  │
   └────┬────┘
        │
        ├────────┬────────┐
        ▼        ▼        ▼
   ┌────────┐ ┌────────┐ ┌────────┐
   │ Task B │ │ Task C │ │ Task D │
   └────┬───┘ └────┬───┘ └────────┘
        │          │
        └────┬─────┘
             ▼
        ┌────────┐
        │ Task E │
        └────────┘
```

**Библиотеки для визуализации:**
- **React Flow** — интерактивный граф
- **Cytoscape.js** — мощная библиотека для графов
- **D3.js** — кастомная визуализация

**Вариант 2: Список зависимостей**
```
┌─────────────────────────────────────────────────────────┐
│  Задача: Настроить CI/CD pipeline                       │
├─────────────────────────────────────────────────────────┤
│  Зависит от:                                            │
│  ✅ Создать Docker образ (завершена)                    │
│  ⏳ Настроить тестовый сервер (в работе)                │
│                                                         │
│  От этой задачи зависят:                                │
│  📋 Автоматический деплой на production                 │
│  📋 Настроить мониторинг                                │
│                                                         │
│  [Добавить зависимость]                                 │
└─────────────────────────────────────────────────────────┘
```

**Автоматизация:**
```python
# Когда Task A завершается
async def on_task_completed(task_id: UUID):
    # Найти зависимые задачи
    dependent_tasks = await db.execute(
        select(Task)
        .join(TaskDependency)
        .where(TaskDependency.depends_on_task_id == task_id)
    )

    for task in dependent_tasks:
        # Проверить, все ли зависимости выполнены
        all_deps_done = await check_all_dependencies_done(task.id)

        if all_deps_done:
            # Уведомить исполнителя: "Можно начинать Task B!"
            await send_notification(
                user_id=task.assignee_id,
                message=f"Задача '{task.title}' разблокирована и готова к работе"
            )
```

**Gantt-диаграмма (опционально):**
Для визуализации зависимостей во времени:
- **DHTMLX Gantt** (платная, но мощная)
- **Frappe Gantt** (open-source, легковесная)

---

### 🔔 Notifications & Real-time

**Статус:** 💡 Idea

- WebSocket для real-time обновлений
- Push-уведомления (Progressive Web App)
- Email дайджесты (ежедневные, еженедельные)
- Telegram/Slack интеграция

### 📊 Analytics & Reporting

**Статус:** 💡 Idea

- Burndown charts (Agile)
- Velocity tracking (сколько задач закрывается в спринт)
- Time tracking (сколько времени на задачу)
- Custom reports (SQL-like query builder для бизнес-аналитиков)

### 🎯 Gamification

**Статус:** 💡 Idea

- Баллы за выполнение задач
- Достижения (badges)
- Leaderboards (топ исполнителей)
- Уровни (Junior → Senior → Lead)

### 🔗 Integrations

**Статус:** 💡 Idea

- **Jira** — импорт/экспорт задач
- **GitHub** — привязка к PR/Issues
- **Google Calendar** — синхронизация дедлайнов
- **Zapier/Make** — автоматизация

### 🧪 Testing & Quality

**Статус:** 💡 Idea

- E2E тесты (Playwright)
- Load testing (Locust)
- Security audit (OWASP)
- Accessibility (WCAG 2.1)

---

## 📝 Template для новых идей

```markdown
## 🎯 [Название идеи]

**Статус:** 💡 Idea
**Дата:** YYYY-MM-DD
**Автор:** [Имя]
**Priority:** Low / Medium / High / Critical
**Effort:** Small (1-2d) / Medium (1w) / Large (2-4w) / XL (1m+)

### Описание
[Что хотим сделать]

### Зачем нужно
[Какую проблему решает]

### Технологии
[Какие технологии использовать]

### Риски
[Что может пойти не так]

### Оценка
[Сколько времени займет]
```

---

**Последнее обновление:** 2026-01-02
